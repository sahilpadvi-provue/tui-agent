/**
 * Turn a title into a URL slug: lowercase, spaces and punctuation collapsed
 * to single hyphens, no leading or trailing hyphen.
 */
export function slugify(title) {
  throw new Error("not implemented");
}
